[project]
name = "binance-breakout-screener"
version = "0.1.0"
description = "No-dependency Binance futures breakout detector for spotting early crypto breakout candidates."
requires-python = ">=3.10"

[project.scripts]
breakout-detector = "screener.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
